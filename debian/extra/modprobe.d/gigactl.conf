# ec_sys exposes /sys/kernel/debug/ec/ec0/io read-only by default; the daemon
# needs to write fan duty and keyboard registers through it. debugfs is mode 0700
# and root-owned, so this grants nothing to unprivileged users.
options ec_sys write_support=1
