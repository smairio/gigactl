# The daemon's preferred Embedded Controller backend is the ec_sys debugfs file,
# where the kernel's own EC driver performs and serialises each transaction. Its
# absence is not fatal — the daemon falls back to raw /dev/port — but that
# fallback races the kernel driver, so load the module at boot.
# Write support comes from modprobe.d/gigactl.conf.
ec_sys
